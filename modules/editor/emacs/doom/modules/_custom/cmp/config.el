;;; _general/cmp/config.el -*- lexical-binding: t; -*-
;;; Commentary:
;;; Code:
(use-package! affe
  :config
  ;; Manual preview key for `affe-grep'
  (consult-customize affe-grep :preview-key "M-."))

;;; config.el ends here
